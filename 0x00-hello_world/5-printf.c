#include <stdio.h>

/**
 * main - Write a C program that prints a sentence followed by a new line
 * using the 'printf' command instead of 'puts' this time around.
 *
 * Return: Always 0 (Succeful)
 */

int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,\n");
	return (0);
}
