#include "main.h"

/**
 * mul - A function that multiplies two integers.
 * @a: first variable
 * @b: second variable
 * Return: Always 0 (To ascertain successful run)
 */

int mul(int a, int b)
{
	int mul;

	mul = a * b;
	return (mul);
}
